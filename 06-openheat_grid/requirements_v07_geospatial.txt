# Additional geospatial dependencies for v0.7 grid feature extraction.
# Install after the base requirements if needed:
#   pip install -r requirements_v07_geospatial.txt
geopandas>=0.14
shapely>=2.0
pyproj>=3.6
pyogrio>=0.7
rasterio>=1.3
matplotlib>=3.7
requests>=2.31
# Optional. If it fails on Windows, use QGIS to export roads/water instead.
# pyrosm>=0.6
